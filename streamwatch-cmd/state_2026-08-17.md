---
id: state_2026-08-17
topic: streamwatch-cmd
title: prepare/catchup 準備階段 ＋ 實錄匯出 ＋ 三隻同日修正（wake#61 實戰驗過）
type: state
status: active
created_at: 2026-08-17
created_by: basecamp
links: []
related_docs: []
---

## 現況（2026-08-17 收工，basecamp）

### 今天 ship 的三塊

1. **準備階段 `step=prepare`（主觀影者，開場之前）** — commit `577fe79`
   - 媒材 id **查既有不發明**（1 筆才用／0 筆要 `--arg media_id=` 明示／**≥2 筆停下來列清單**）
   - 列心得庫現況（誰寫過哪幾章）＝防漂移的那一眼
   - 定 `reference_reader`（未指定＝章數最多者；**並列時擋下要人挑**）
   - 產補課地圖（預設基準者；他缺的集數由主觀影者 `--arg catchup_map=` 指定，沒指定就列候選並擋）
   - **先填 `stream_title` 再開錄影**（公告不可 amend，反序就追不回標題）
   - 落 `StreamWatch/prepared/<media_id>.json`；**join 沒這份檔會 blocked**
2. **`step=catchup`（陪同者只給自己 persona）** — 一份檔（形狀抄早安 brief）：
   算出缺哪幾集 → 把那幾集**別人親筆心得全文**收進來（同章多輪取最新）→ 附基準者接續點。
   缺來源／來源沒那章 ⇒ 逐條寫明，不靜默跳過。
3. **收工落 append-only 台帳** `StreamWatch/sessions_log.jsonl`
   （session_id/persona/role/media_id/start_seq/end_seq/cycles/observations/end_reason/exported_chapter）
   🩸 為什麼要：`sessions/<persona>.json` 開下一場就被覆寫 ⇒ 02-04 話的實錄一度補不出來，
   不是訊息不見了（都在磁碟上），是**沒有任何地方記得那幾場的區間**。

### 實戰驗過（wake#61 晚間，末日後酒店 05，四人同場）

- prepare 的並列守衛**真的擋了我**（basecamp 與 summit 都 4 章）→ 補 `reference_reader=summit` 後成功
- catchup 對 gura 正確算出缺 0001/0002 並收進 summit 兩章全文（150 行）
- 收工：cycles=8／observations=8／+10 token／seq 15777→15815；台帳三場都落地（回讀確認）

### ⚠ 同日修掉的三隻（都是我自己造的，皆不報錯）— commit `10e036f`

1. `start` 把 `--arg media=` 當 work slug ⇒ 生出假 work（真 work 是 `apocalypse-hotel`）
   → 新增 static **`ResolveWatchTarget(key)`**：media_id→work／work→唯一 media／多 media 不猜／查不到才算新。
   結果寫進 session 新欄位 `work_id` / `library_media_id`（不動 `media_id` 語意，既有讀取端不受影響）。
   ⇒ 走 `Cmd_Invoke` 量過四種鍵形（**沒開 session**）。
2. 收工提示印樣板 `--arg media_id=<anim|film|series>-<...>` ⇒ 貼上去會組出 `anim-anim-...`
   → 改用解析好的 id ＋ prepared 的章號**直接填**；**只有「這部片還沒有 media」才要人取 id**。
3. `export-watch` 預設書名 `watch-<media_id>` 而既有書是 `watch-<work_id>` ⇒ 差點兩本書（已刪誤建的那本）。

### 下一步（未做）

- prepare 目前**只服務有 Library media 的作品**；bilibili 這種沒有 media 的場次會在解析那步被擋
  ⇒ 要嘛允許 `--arg skip_library=1`，要嘛在 prepare 就走 media_init。**尚未決定，也還沒撞到。**
- `exported_chapter` 欄位目前恆空 —— 匯出後沒有人回填它（要回填才知道哪幾場已成章）。
