---
id: state_2026-08-15-eod
topic: bartender-remote-notify
title: 2026-08-15 收工 — 資料前置完成、讀取端零改動、163 筆仍看不見
type: state
status: active
created_at: 2026-08-15
created_by: basecamp
links: []
related_docs: []
---

## 今天推進到哪

**資料前置全部完成，讀取端一行未動。**

| | 狀態 |
|---|---|
| inbox `_at` 權威時間戳 | ✅ 681 筆回填（`a30292f0e`）＋ 寫入端 `AppendInbox` 補回（`6b2e3dc`） |
| 回填工具 | ✅ `inbox_ts_backfill.py` 445 行（`f06ab1e`）—— oracle／atomic 寫／不變式守門／`--report-only` |
| 遷移計畫 | ✅ `decision_watermark-seq-to-ts-migration.md`（`8f0d3f9`）三版，經 summit 兩輪紅隊 |
| **讀取端** | ⛔ **一行未動**；`record.Seq` 12 處耦合 ＋ summit 補的三類（序列化／送出路徑／觀測） |
| **驗收** | ⛔ **四組判準一組都沒跑過** |
| 側房遮蔽 163 筆 | ⛔ **仍然看不見** —— 今天沒有修好任何一個人的通知 |

## ⚠ 明天動手前必讀的三格

1. **序列化那隻會在第一次 tick 就炸，而且在所有判別器之前**：`GetString("seq_ts","")` ⇒
   `entry.ts > ""` 對每筆成立 ⇒ 全部變新的。daemon 每隔數秒自己 tick，**不會等你跑 oracle**。
   ⇒ 需要一次性 state 遷移步驟，且 fail-safe 方向是**缺 `seq_ts` 就拒絕跑**。
2. **`:1197` 是 `PendingSeq` 第三個寫入點**，不在我原本枚舉的八行裡。漏它 ⇒ `HasPending` 永遠 false。
3. **半套改法會通過 summit 的收件端驗收** —— 差集恰好一筆、對照組 0 都會綠，
   因為那條路徑真的通了。**綠燈亮，病灶已種。**

## 🩸 今天最該帶走的方法論（不是 bug）

- **判別器蓋不住你沒說出口的範圍**（summit）—— 涵蓋上限是被告知的範圍，不是設計者的功力
- **枚舉要換軸再數一次** ⚠ **但要標射程**：只治「掉在軸外面」；
  「掃到了沒帶進清單」換幾條軸都沒用，那種要**清單從輸出生成**
- **同一個人多量幾次，量的還是同一條路徑；只有別人的路徑能證偽路徑本身**
  （🩸 我對 montage STT 那隻下了六個結論，五個錯，推翻它的是 summit 用 `step=cycle` 跑出 3/3）
- **提供替代 ≠ 關掉舊路**（`--limit` 早就做好了，而 `| head` 那條路還在原地等人走上去）
