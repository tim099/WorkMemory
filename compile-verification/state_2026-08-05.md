---
id: state_2026-08-05
topic: compile-verification
title: 新鮮度守衛 + 停跳台帳 + recompile 完成條件 全部落地並實測；LY parent bump 待 Tim
type: state
status: superseded
created_at: 2026-08-05
created_by: summit
links: [compile-verification/state_2026-08-05-eod]
related_docs: []
---

**施工現況**（2026-08-05 10:15 快照 — 過期時 supersede 本檔，別改寫）

**已完成並實測**
- `check_compile.py` 新鮮度守衛：STALE 橫幅 / 「Clean compile」在過期時消失改印「無法判定」/
  `--since-file` `--since` `--strict-fresh` `--no-freshness` / json 帶 stale 欄位（`8357d7c`）
- `UCL_BartenderIO` 停跳台帳 `_heartbeat_stalls.jsonl`，gap ≥ 3s、ring 10（`8357d7c`）
- `run_cmd.py recompile` 完成條件改成「mtime 推進 + in_progress=false」，超時訊息拆兩種（`ba5ccc7`）
- 台帳 + .tmp 進 `bartender/.gitignore`（`84e08034`）
- skill 文件更新 + 三份安裝副本（`.claude`/`.codex`/`.agents`）已用 `install_skills.py` 同步

**測試**：check_compile 16 項、recompile 5 項，正反兩向都測。
其中兩項第一版是**測試自己寫錯**（ring 裁切那次沒真的發生停跳；recompile 卡住那條前置條件搭錯）——
**「測了沒紅」不等於「測到了」**，兩次都改成確定性注入才真的打到目標路徑。

**pending**
- 主專案（LY）parent bump 未做：`AgentCommands`(`84e08034`) 與 `Assets/Plugins/UCL_Core`(`ba5ccc7`)
  兩個指標、以及 `.claude`/`.codex`/`.agents` 的 ucl-compile-error 安裝副本都還沒進 LY commit。
  原因：LY 的 index 裡有 Tim 正在進行的 stage（ucl-ding 三份 SKILL.md / Docs~Glossary），
  在那裡提交會把別人的東西一起掃走且無錯誤訊息 —— 等 Tim 點頭。**全部未 push。**
- 已知盲區未解（刻意接受，寫在 code 註解裡）：已 commit 但沒編譯的情況偵測不到；
  只看 mtime 不看內容（touch 也算改動，誤判方向偏保守）
- 可考慮：`--editor-alive` 是否該併印「最近停跳」以免單看心跳誤判為「一切正常」
