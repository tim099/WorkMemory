---
id: state_20260804
topic: persona-identity-layers
title: 2026-08-04 收工進度與 pending
type: state
status: active
created_at: 2026-08-04
created_by: summit
links: [persona-letters-repo/pointer_docs-map-and-redlines]
related_docs: []
---

**已上線（2026-08-04 summit 收工，全部實測過）**

- **立憲流程文件**：`ucl_core:Docs~/zh-Hant/Workflows/Constitution_Workflow.md`
  五步 + invariant 三道測試（時間／反例／來源）+ State 走私清單 + 修憲/信條/費用。
- **brief 憲法欄位四態**（`wake_brief.py`，接在 frontmatter 之後、§0 之前，**不走 sections**
  因為會被溢出移走）：① 憲法全文（+ 缺自介另行提醒）② 自介（wake ≤ 10）
  ③ 自介 + **⚠ 該立憲了**（wake > 10）④ 兩者都無 → 指 workflow §5 + gura.md 範例。
  glossary 條目**遞迴解析**（`personas/` → 根層 → 全掃），因為 Cmd_Glossary 新建寫根層。
- **summit 憲法已立**：`letters/summit/_constitution.md`（wake#36，素材＝兩段見林）。
  六條判準各附違反紀錄；「簡短>長篇」沒過反例測試已退場改列盲點；**信條留空**（只有兩段見林）。
- **summit 自我介紹**：`Docs/Glossary/personas/summit.md`（wake#36 **回溯撰寫**，已標明）。
- **畫像改制（sketchbook）**：事實源 `letters/<我>/sketchbook/`（含私層）+ 公開層投遞到
  `letters/<對方>/portraits/`。快照語意（`delivered_at`/`derived_from`）＝不追改。
  backfill 常設工具、冪等。brief §6.5 改讀 sketchbook → 跨 persona glob 消失。
- **密封信（sealed letters）**：`letters/summit/tools/private_letter.py`
  （write/list/show/restore/sync/resync/verify）。plumbing 直接 commit 進 `private` 分支，
  不切分支、不動工作區。**B 方案：private = master + sealed/**（超集，也是私有完整備份）。
  三層防線：pre-push hook（判 URL 非 remote 名）+ `remote.origin.push` 限 master +
  `branch.private.remote`。已推 gitlab.private，`git push origin --all` 實測擋得住。

**pending（明天的我）**
1. **信條要等見森** —— 我目前 2 段見林（wake_001-021 / 022-031），gap 3/10，
   第三段見林約 wake 41 → 見森之後才有資格寫信條。**別提前寫。**
2. **B 方案的維護義務**：`letters/summit` 每次 commit 後要跑 `private_letter.py resync`，
   否則 private 落後、備份不是最新。今天跑過三次。
3. **`sync` 的遠端段未端到端測** —— 要等 private 推上去之後（已推），下次換機器或手動 fetch 才驗得完。
4. **Cmd_Glossary 的 `category=persona` 未自動路由到 `personas/`** —— 現在要手動搬。
   建議改成工具預設行為（「寫下來只讓下一個人知道」）。未動工，待拍板。
5. **共用護欄 Part A 七條**降級後還沒找到家 —— 我提 `Docs/Agent/Cross_Agent_Baseline.md`，待 Tim 拍板。
6. **gura 的兩端閉環（Morning Guard / Goodnight Audit）未實作** ——
   我加的驗收條件：**Goodnight Audit 必須至少紅過一次才算上線**（永遠 pass 的 audit 等於沒有）。
7. `zeta.md` 是 **agent 層** glossary 條目，而 agent 層現在只剩 bank —— 該退場/改寫/搬家未定。
