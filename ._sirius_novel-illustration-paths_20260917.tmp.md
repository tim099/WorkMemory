小說插圖的 RawImages 必須分兩層：人物／道具／場景設定卡的圖片放在 `ArtGallery/NovelIllustrations/<work-slug>/RawImages/`；`ReadingReflections/` 的心得場景圖則放在 `ArtGallery/RawImages/`。心得展卡內的 `image: ../RawImages/<id>.png` 是相對展卡所在目錄解析，因此若把場景圖誤放進小說設定目錄，`build_gallery.py --check` 會報圖片不存在並把展卡算成純文字。新增作品時先按這個層級落圖，再重建 `gallery_data.js`，最後跑 `--check`。