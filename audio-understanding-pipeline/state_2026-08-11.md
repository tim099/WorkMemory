---
id: state_2026-08-11
topic: audio-understanding-pipeline
title: 調研完成、未動工 — Tim 要花時間想方案；關鍵路徑已改成『先量便宜的』
type: state
status: active
created_at: 2026-08-11
created_by: basecamp
links: []
related_docs: [ucl_core:Docs~/zh-Hant/Plan/Plan_Audio_Understanding_Pipeline.md]
---

【進度】2026-08-11 早上調研成文（§0-§7），當晚陪看時在真實素材上量到血證（§2.2b），
並經 Tim 需求補充與 summit / Sirius 兩輪砸磚大幅修訂（§8，13 節）。
commit `9c7682d`（UCL_Core 單層，**父層未 bump，同事 pull 主專案還拿不到**）。

【現行關鍵路徑（§8.11，取代已作廢的 §5）】
1. 換 faster-whisper（保留 openai-whisper 可回退）
2. 加 `stt_vad_filter` 後台開關 —— 落點是 `UCL_ScreenStreamPage.cs:999-1014`「靜音幻覺防治門檻」區塊
   （**不是 MediaAdminPage，早上寫錯已更正**；同族三個旋鈕 stt_rms_gate/no_speech_max/logprob_min 都在那）
3. A/B 實測：同素材、關 vs 開 vad_filter，數片尾型幻聽筆數
4. 依結果決定分離層是核心還是加分項專用 ← **先量便宜的，再決定要不要蓋貴的**
5. 若要蓋分離 → 先跑 summit 的第 0 號實驗（合成混音 → 分離 → 抽 F0 對 ground truth），未過不准續建
6. Stage 0 capture/process 解耦（無論走哪條路都要做；不做的話插分離層會把漏聽從 5% 推到 40% 且無聲）

【驗收基準線（現場量到，可複驗）】
片尾型幻聽 **17 分鐘 9 筆、約每 2 分鐘一次**（下界，當輪 STT cache 曾落後）。
上線後拿同素材重跑比對；**頻率沒明顯下降＝沒修好**，不接受主觀判斷。

【⛔ 已知的錯，別重犯】
- `stt_prompt` 餵人名清單 **反效果**（我提的）：whisper 在非語音段把清單當台詞吐出，
  比原本的專名崩壞危險（假訊號像真台詞、無法黑名單濾、毒害語者推理）。
  根因＝`initial_prompt` 與 `condition_on_previous_text` 同機制，而本專案早就關掉後者防幻覺滾雪球。
  **Tim 已清空該欄位，三輪確認未再復發。** 詞彙偏置改走「事後對照名單校正」。
- 提任何「改個設定就有效」的方案前，**先 `grep -i <關鍵字> AgentCommands/Lessons/lessons.jsonl`**
  （158 條，一行指令）—— 這次的現象七月就在 summit 那條教訓的 context 欄裡了。

【pending / 待決】
- **Tim 說先不動工，他要花時間想方案 —— 等他拍板才動 code。**
- §8.12 列了 10 條未驗，**一條都沒在本機跑過**
- MOSS-Transcribe-Diarize 0.9B（單趟 90 分鐘 + hotwords + Apache-2.0）值得試，
  但**對配樂的耐受度官方完全沒寫**，且端到端 LM 不吐 embedding → 切塊時無法做語者置換對齊
- Streaming Sortformer 只做 diarization（不碰 BGM/語氣）；**4 語者上限是懸崖**
  （≤4 人 DER ~13% → 5+ 人 ~42-43%）。建議降級成「語者變更點偵測」，
  ⚠ 但那是**我的推論非官方陳述**，是本案最弱的一環
- 聲紋名冊要做成 **sketchbook 形狀**（版本化猜測、不覆寫），因為 Tim 要「觀看中推理」的樂趣
